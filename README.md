# Check Insurance Risk — Frontend (Clean)

## Configure
Set environment variable:

- `VITE_API_URL=https://check-insurance-risk-backend-railway.onrender.com`

## Local
```bash
npm install
npm run dev
```

## Netlify
Build command: `npm run build`  
Publish directory: `dist`  
Env var: `VITE_API_URL`
